# 项目状态快照（保持短小：建议 <= 200~400 行）

## 当前结论（必须最新）
- 现状：Azure TTS 针对字段内 `&nbsp;` 等 HTML 实体的 `HTTP 400` 已完成修复；插件现在会先规整文本，再本地校验 SSML，避免把非法 XML 发到 Azure。
- 已完成：确认运行日志位置为 `user_files/logs/csv_importer.log`；复现出 `note_id=1779258030286` 因 `abductor pollicis longus&nbsp;` 触发 `HTTP 400`；为 TTS 文本增加 HTML 实体解码、HTML 标签清理、XML 转义；新增发送前 SSML 本地校验；补充并通过 8 条单元测试；用真实问题文本完成最小 SSML 渲染与 XML 解析验证。
- 正在做：暂无。
- 下一步：在 Anki 里重新对 `note_id=1779258030286` 执行一次 TTS，确认现场也成功生成音频并写回 `[sound:...]` 标记。

## 关键决策与理由（防止“吃书”）
- 决策A：扫描阶段输出分类统计，而不是只显示总任务数。（原因：用户看到 325 张卡却出现 400 多条“待生成”时，需要知道差异来自候选范围、复用媒体、已有标记还是异常笔记。）
- 决策B：日志放在 `user_files/logs/csv_importer.log`。（原因：`user_files/` 已被 `.gitignore` 忽略，适合保存本机运行日志且不会误提交用户数据。）
- 决策C：HTTP 错误不记录 Key 等敏感请求头，只记录方法、URL、状态码或底层原因。（原因：排查网络/配置问题需要 URL，但不能把密钥写进日志。）
- 决策D：logger 不注册 `StreamHandler`，并在初始化时清理旧 handler。（原因：Anki 会捕获插件写到标准输出/错误流的内容，并可能弹出“插件遇到问题”。）
- 决策E：TTS 文本先做 HTML 实体解码与 XML 转义，再嵌入 SSML。（原因：Azure TTS 的 REST 接口接收的是 `application/ssml+xml`，`&nbsp;` 属于 HTML 实体，不是 XML 预定义实体，直接发送会得到 `400`。）
- 决策F：SSML 在发请求前先本地做 XML 合法性校验。（原因：比线上只返回一个空 `400` 更容易定位到具体文本或模板。）

## 常见坑 / 复现方法
- 坑1：只在树状牌组里点选目标牌组，但没有勾选“限制牌组范围”，扫描不会按牌组过滤。
- 坑2：Anki 的“卡片数”和插件扫描的“笔记任务数”不是同一层概念；一个笔记可能生成多张卡片，TTS 当前按笔记生成音频。
- 坑3：`urlopen error [Errno 2] No such file or directory` 发生在 HTTP 请求阶段，不代表卡牌或笔记不存在；日志里会保留对应的请求 URL 与底层错误原因。
- 坑4：勾选“仅处理最近导入的笔记”但最近导入 ID 为空时，输入会是 0；当前已在选中牌组时自动回退到牌组扫描。
- 坑5：Anki 字段中常带 HTML 实体或标签；若把原文直接塞进 SSML，像 `&nbsp;` 这种 HTML 实体会让 XML 解析失败，Azure 往往只回 `400`。
